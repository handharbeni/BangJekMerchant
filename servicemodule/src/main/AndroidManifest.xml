<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="illiyin.mhandharbeni.servicemodule">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <permission-group
        android:name="android.permission-group.LOCATION" />
    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">
        <service android:name=".service.MainService"/>
        <service android:name=".service.intentservice.CategoryMenuService"/>
        <service android:name=".service.intentservice.CategoryMerchantService"/>
        <service android:name=".service.intentservice.MenuMerchantService"/>
        <service android:name=".service.intentservice.MerchantInformationService"/>
    </application>
</manifest>
